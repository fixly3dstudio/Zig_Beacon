services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: beacon
      POSTGRES_PASSWORD: beacon
      POSTGRES_DB: beacon
    ports:
      - "5433:5432"
    volumes:
      - beacon-pgdata:/var/lib/postgresql/data
volumes:
  beacon-pgdata:
